<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/output.css">
</head>
<body class="bg-slate-950 text-white flex items-center justify-center flex-col">
   
    <div class="my-4 h-12 w-full flex items-center justify-center text-4xl">
        Mini Message Board
    </div>
    <ul class="bg-purple-200 w-[80vw] min-h-screen flex flex-col gap-5 rounded-xl overflow-y-auto p-4">
        <li class="flex text-black self-end m-4">
            <a href="/messages" class="bg-blue-500 h-10 w-24 rounded-xl flex items-center justify-center">Confess</a>
       
        </li>
        <% messages.forEach(message => { %>
            <li class="h-[16vh] flex flex-row justify-between text-black bg-purple-300 w-[90%] m-5 p-5 rounded-xl">
            <div class="flex flex-col">
                <strong><%= message.user %> : </strong> <%= message.desc%>
            </div>
            <div class="flex flex-col h-full gap-4">
                <div>
                    <button class="bg-red-400 h-8 w-24 self-end rounded-xl">
                        <a href="/messages/<%= message.text%>">Like</a>         
                    </button>
                    <button class="bg-blue-500 h-8 w-24 self-end rounded-xl">
                        <a href="/messages/<%= message.uid%>">Open</a>
                    </button>
                </div>
                
                <span class="text-gray-600">(@ <%= message.added.toDateString() %>)</span>
            </div>
            
            </li>
        <% }); %>
    </ul>
    
</body>
</html>